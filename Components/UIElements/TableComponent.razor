@* TableComponent.razor *@
@if (ElementsToStore.Count > 0)
{
    @* <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th scope="col">Index</th>
                <th scope="col">Element</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < ElementsToStore.Count; i++)
            {
                <tr>
                    <th scope="row">@i</th>
                    <td>@ElementsToStore[i]</td>
                </tr>
            }
        </tbody>
    </table> *@

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th scope="col">Item</th>
                <th scope="col"></th>
            </tr>

        </thead>
        <tbody>
            @for (int i = 0; i < ElementsToStore.Count; i++)
            {
                <tr>
                    <th scope="row">@Labels[i]</th>
                    <td>@ElementsToStore[i]</td>
                </tr>
            }
        </tbody>
    </table>

}
else
{
    <p>No elements to display.</p>
}

@code {
    [Parameter]
    public List<string> ElementsToStore { get; set; }

    List<string> Labels = new List<string>()
{
    "Proceso",
    "Corte",
    "Fecha",
     "OPIS confirmadas",
     "Monto Total",
};


}
